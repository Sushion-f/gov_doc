# Python
__pycache__/
*.py[cod]
*.pyo
.Python

# 运行期日志（目录保留，见 runtime_logs/.gitkeep）
runtime_logs/*.log
runtime_logs/*.log.*

# 本地工作区缓存（不上库）
.runtime-data/**/workspace/.cache/
.runtime-data/**/workspace/.versions/

# 本地虚拟环境（若建在项目内）
.venv/

# SQLite 本地库（按需保留；若需入库可删此行）
# *.db
# *.db-shm
# *.db-wal
